// The actual Tailwind v4 `@theme` mapping lives in the sibling `preset.css` file,
// imported directly by consumers: `@import "@dynamong/theme/tailwind-preset/preset.css";`
export * from './lib/mapped-theme-keys';
