# theme-aura

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build theme-aura` to build the library.

## Running unit tests

Run `nx test theme-aura` to execute the unit tests via [Vitest](https://vitest.dev/).
