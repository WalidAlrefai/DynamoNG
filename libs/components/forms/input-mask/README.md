# @dynamong/input-mask

A masked text input that enforces a fixed character pattern as the user types — literal characters auto-insert, backspace/delete correctly skip over them, and paste re-applies the mask.

Run `nx test forms-input-mask` to execute the unit tests.
