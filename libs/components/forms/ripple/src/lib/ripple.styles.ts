// The one Tailwind class string for this directive — applied to the ripple
// `<span>` it appends. `bg-current/30` tints against whatever text colour the
// host resolved to; `dgRippleColor` overrides it with an inline background.
export const rippleSpanStyles =
  'pointer-events-none absolute z-0 rounded-full bg-current/30';
